тесты формы:
- не выбран файл
- не введен хотя бы один коэффициент
- если статус будет не оптимальным (выдает все нормально)

тесты эксель:
- не все поля заполнены
- неверная структура таблицы
- попадаются стринги
- 0 товаров
- 1 товар
- v, V и Т не инты
- интеграл в тета

тесты проги:
- имя файла кириллицей

--------------------------------------------------------------------------------------------------

сделать форма:
- вставить новую картинку с формулами
- захерачить второй таб
- иконка
- лэйблы по размеру текста
- "устойчивость решения"
- второе уравнение (D - 0,4F, y - 0,12 умолчание)

сделать эксель:

сделать прога:
- провести анализ в pycharm
- создать массив для статусов
- является ли ноль - оптимальным?
- счетчик подзадач (написать ручками рекурсию по бинарному дереву)
- def test()
- не округлять переменную
-
